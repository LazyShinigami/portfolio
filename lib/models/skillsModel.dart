class SkillModel {
  SkillModel({required this.name, required this.level, required this.category});
  String name, category;
  int level;
  // SkillModel.fromData(Map data) {
  //   this.name = data['skillName'];
  //   this.level = data['skillLevel'];
  //   this.category = data['skillCategory'];

  // }
}
